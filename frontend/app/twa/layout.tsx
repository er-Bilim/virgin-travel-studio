export default function TwaLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="twa-container">{children}</div>
    // Здесь я пока не знаю что должно быть. В будущем что-нибудь придумаем
  );
}